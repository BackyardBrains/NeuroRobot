- release_tracks: [ALPHA]
  help_text:
    brief: Add an IAM policy binding to a Cloud Spanner database.
    DESCRIPTION: |
      Add an IAM policy binding to a Cloud Spanner database.
  request:
    collection: spanner.projects.instances.databases

  arguments:
    resource:
      help_text:
        The Cloud Spanner database for which to add IAM policy binding to.
      spec: !REF googlecloudsdk.command_lib.spanner.resources:database
