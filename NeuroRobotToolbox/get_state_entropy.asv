


state_entropy = zeros(n_unique_states, 1);
sort_inds = [];
for nstate = 7:n_unique_states
    these_inds = state_inds(nstate, :);
    sort_inds = [sort_inds ;
    these_corrs = corr(xdata(these_inds,these_inds));
end